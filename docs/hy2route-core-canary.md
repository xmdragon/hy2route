# hy2route-core canary

The canary uses `inet hy2route_canary`, DNS port 2053 and TCP/UDP port 22345. It must include an explicit `canary_source`; all other LAN sources return before the active chain. Production files and the legacy service remain unchanged until the canary record is accepted.
