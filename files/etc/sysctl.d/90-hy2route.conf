# Raise the per-socket ceiling for the single long-lived HY2 QUIC connection.
# Defaults remain unchanged so unrelated sockets do not reserve extra memory.
net.core.rmem_max=4194304
net.core.wmem_max=4194304
